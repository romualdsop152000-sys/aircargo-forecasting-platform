apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: aircargo
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: password
  POSTGRES_DB: aircargo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: aircargo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: postgres-secret
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: aircargo
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
