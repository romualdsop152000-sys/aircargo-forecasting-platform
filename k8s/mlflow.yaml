apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlflow
  namespace: aircargo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mlflow
  template:
    metadata:
      labels:
        app: mlflow
    spec:
      containers:
        - name: mlflow
          image: ghcr.io/mlflow/mlflow:v2.13.0
          command:
            - mlflow
            - ui
            - --host
            - 0.0.0.0
            - --port
            - "5000"
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: aircargo
spec:
  selector:
    app: mlflow
  ports:
    - port: 5000
      targetPort: 5000
